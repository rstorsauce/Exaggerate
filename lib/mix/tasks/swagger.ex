defmodule Mix.Tasks.Swagger do
  use Mix.Task

  @shortdoc "generates an api from the supplied swaggerfile(s)"
  def run(_params) do
  #  [swaggerfile | _options] = params
#
  #  unless File.exists?(swaggerfile) do
  #    Mix.raise("no file error")
  #  end
#
  #  swaggerfile
  #  |> Path.expand
  #  |> File.read!
  #  |> Jason.decode!
  #  |> Exaggerate.to_code!
  #  |> IO.inspect
#
  end
end
